CREATE TABLE IF NOT EXISTS media_insights (id INTEGER PRIMARY KEY AUTOINCREMENT, media_id TEXT NOT NULL, kind TEXT NOT NULL, title TEXT, posted_at TEXT, collected_date TEXT NOT NULL, collected_at TEXT, reach INTEGER, views INTEGER, likes INTEGER, comments INTEGER, shares INTEGER, saved INTEGER, replies INTEGER, total_interactions INTEGER, raw TEXT, UNIQUE(media_id, collected_date));
